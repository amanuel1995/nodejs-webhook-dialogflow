extends layout

block content
    h1.
        QRG List
    //- ul
    //-     each user, i in userlist
    //-         li
    //-             //- a(href="mailto:#{user.email}")= user.username 
    //-             //- #{user.email}= user.username
    //-             #{user.manufacturer}= user.terminalType
    


    ul
        for QRG in userlist
            li= QRG.manufacturer
            li= QRG.terminalType
        else
            li sorry, no books!

